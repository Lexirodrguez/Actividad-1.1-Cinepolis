<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/">CinePolis</a>
            <div class="navbar-nav">
                <a class="nav-link" href="/peliculas">Películas</a>
                <a class="nav-link" href="/salas">Salas</a>
                <a class="nav-link" href="/funciones">Funciones</a>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <h1>Gestión de Salas</h1>
        
        <div class="row mt-4">
            <% salas.forEach(sala => { %>
            <div class="col-md-4 mb-4">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title"><%= sala.nombre %></h5>
                        <p class="card-text">
                            <strong>Capacidad:</strong> <%= sala.capacidad %> personas<br>
                            <strong>Tipo:</strong> <%= sala.tipo %><br>
                            <strong>Estado:</strong> 
                            <span class="badge <%= sala.activa ? 'bg-success' : 'bg-danger' %>">
                                <%= sala.activa ? 'Activa' : 'Inactiva' %>
                            </span>
                        </p>
                    </div>
                </div>
            </div>
            <% }); %>
        </div>
    </div>
</body>
</html>