<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/">CinePolis</a>
            <div class="navbar-nav">
                <a class="nav-link" href="/peliculas">Películas</a>
                <a class="nav-link" href="/salas">Salas</a>
                <a class="nav-link" href="/funciones">Funciones</a>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="row">
            <div class="col-md-8">
                <h1><%= pelicula.titulo %></h1>
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">Información de la Película</h5>
                        <p class="card-text">
                            <strong>Género:</strong> <%= pelicula.genero %><br>
                            <strong>Duración:</strong> <%= pelicula.duracion %> minutos<br>
                            <strong>Clasificación:</strong> <%= pelicula.clasificacion %><br>
                            <strong>Director:</strong> <%= pelicula.director %><br>
                            <strong>Fecha de Estreno:</strong> <%= pelicula.fechaEstreno %><br>
                            <strong>Estado:</strong> 
                            <span class="badge <%= pelicula.activa ? 'bg-success' : 'bg-danger' %>">
                                <%= pelicula.activa ? 'Activa' : 'Inactiva' %>
                            </span>
                        </p>
                        <a href="/peliculas" class="btn btn-secondary">Volver a Películas</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>